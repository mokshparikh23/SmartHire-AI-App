// HARDENED 2026-09-06: packages/* had no lint config at all before this. See
// packages/config/eslint-package.mjs for why these use the plain rule set
// rather than the Next preset.
export { default } from '@smarthire/config/eslint-package'
